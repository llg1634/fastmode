<Application x:Class="FastMode.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
  <Application.Resources>
    <ResourceDictionary>
      <!-- PCL-like blue scale + grays (fixed theme, no animation engine) -->
      <Color x:Key="ColorObject1">#343D4A</Color>
      <Color x:Key="ColorObject2">#0B5BCB</Color>
      <Color x:Key="ColorObject3">#1370F3</Color>
      <Color x:Key="ColorObject4">#4890F5</Color>
      <Color x:Key="ColorObject5">#96C0F9</Color>
      <Color x:Key="ColorObject6">#D5E6FD</Color>
      <Color x:Key="ColorObject7">#E0EAFD</Color>
      <Color x:Key="ColorObject8">#EAF2FE</Color>

      <SolidColorBrush x:Key="ColorBrush1" Color="{StaticResource ColorObject1}"/>
      <SolidColorBrush x:Key="ColorBrush2" Color="{StaticResource ColorObject2}"/>
      <SolidColorBrush x:Key="ColorBrush3" Color="{StaticResource ColorObject3}"/>
      <SolidColorBrush x:Key="ColorBrush4" Color="{StaticResource ColorObject4}"/>
      <SolidColorBrush x:Key="ColorBrush5" Color="{StaticResource ColorObject5}"/>
      <SolidColorBrush x:Key="ColorBrush6" Color="{StaticResource ColorObject6}"/>
      <SolidColorBrush x:Key="ColorBrush7" Color="{StaticResource ColorObject7}"/>
      <SolidColorBrush x:Key="ColorBrush8" Color="{StaticResource ColorObject8}"/>

      <SolidColorBrush x:Key="ColorBrushGray1" Color="#404040"/>
      <SolidColorBrush x:Key="ColorBrushGray2" Color="#737373"/>
      <SolidColorBrush x:Key="ColorBrushGray3" Color="#8C8C8C"/>
      <SolidColorBrush x:Key="ColorBrushGray4" Color="#A6A6A6"/>
      <SolidColorBrush x:Key="ColorBrushGray5" Color="#CCCCCC"/>
      <SolidColorBrush x:Key="ColorBrushGray6" Color="#EBEBEB"/>
      <SolidColorBrush x:Key="ColorBrushGray7" Color="#F0F0F0"/>
      <SolidColorBrush x:Key="ColorBrushGray8" Color="#F5F5F5"/>

      <SolidColorBrush x:Key="ColorBrushWhite" Color="#FFFFFF"/>
      <SolidColorBrush x:Key="ColorBrushCard" Color="#F5FFFFFF"/>
      <SolidColorBrush x:Key="ColorBrushRedLight" Color="#FF4C4C"/>
      <SolidColorBrush x:Key="ColorBrushRedDark" Color="#CE2111"/>
      <SolidColorBrush x:Key="ColorBrushBgContent" Color="#F7F9FC"/>

      <!-- aliases used by existing code -->
      <SolidColorBrush x:Key="BgBrush" Color="#F7F9FC"/>
      <SolidColorBrush x:Key="CardBrush" Color="#F8FFFFFF"/>
      <SolidColorBrush x:Key="BorderBrush" Color="#D5E6FD"/>
      <SolidColorBrush x:Key="TextBrush" Color="#343D4A"/>
      <SolidColorBrush x:Key="MutedBrush" Color="#8C8C8C"/>
      <SolidColorBrush x:Key="AccentBrush" Color="#1370F3"/>
      <SolidColorBrush x:Key="AccentSoftBrush" Color="#E0EAFD"/>
      <SolidColorBrush x:Key="DangerBrush" Color="#CE2111"/>

      <Style TargetType="Window">
        <Setter Property="FontFamily" Value="Segoe UI, Microsoft YaHei UI, Microsoft YaHei"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Foreground" Value="{StaticResource ColorBrush1}"/>
      </Style>

      <!-- MyCard-like: small radius, light shadow tied to ColorObject1 -->
      <Style x:Key="CardBorder" TargetType="Border">
        <Setter Property="Background" Value="#F5FFFFFF"/>
        <Setter Property="BorderBrush" Value="#66D5E6FD"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="5"/>
        <Setter Property="Padding" Value="14,12"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Effect">
          <Setter.Value>
            <DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.12" Color="#343D4A"/>
          </Setter.Value>
        </Setter>
      </Style>

      <Style x:Key="PrimaryButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource ColorBrush3}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="14,7"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="Button">
              <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="3" Padding="{TemplateBinding Padding}">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                  <Setter TargetName="bd" Property="Background" Value="{StaticResource ColorBrush2}"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                  <Setter TargetName="bd" Property="Opacity" Value="0.5"/>
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <Style x:Key="GhostButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource ColorBrush1}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ColorBrush5}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="Button">
              <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3" Padding="{TemplateBinding Padding}">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                  <Setter TargetName="bd" Property="Background" Value="{StaticResource ColorBrush8}"/>
                  <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource ColorBrush4}"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                  <Setter TargetName="bd" Property="Opacity" Value="0.5"/>
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <Style x:Key="TitleIconButton" TargetType="Button">
        <Setter Property="Width" Value="28"/>
        <Setter Property="Height" Value="28"/>
        <Setter Property="Margin" Value="0,0,8,0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="Button">
              <Border x:Name="bd" Background="Transparent" CornerRadius="14" Width="28" Height="28">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                  <Setter TargetName="bd" Property="Background" Value="#33FFFFFF"/>
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <Style x:Key="ChipButton" TargetType="ToggleButton">
        <Setter Property="Background" Value="{StaticResource ColorBrush8}"/>
        <Setter Property="Foreground" Value="{StaticResource ColorBrush1}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ColorBrush5}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,5"/>
        <Setter Property="Margin" Value="0,0,8,8"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="ToggleButton">
              <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="1" CornerRadius="3" Padding="{TemplateBinding Padding}">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsChecked" Value="True">
                  <Setter TargetName="bd" Property="Background" Value="{StaticResource ColorBrush6}"/>
                  <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource ColorBrush3}"/>
                  <Setter Property="Foreground" Value="{StaticResource ColorBrush2}"/>
                  <Setter Property="FontWeight" Value="SemiBold"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                  <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource ColorBrush4}"/>
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <Style x:Key="FieldTextBox" TargetType="TextBox">
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="BorderBrush" Value="{StaticResource ColorBrush5}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Background" Value="{StaticResource ColorBrush8}"/>
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="TextBox">
              <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3">
                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsFocused" Value="True">
                  <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource ColorBrush3}"/>
                  <Setter TargetName="bd" Property="Background" Value="White"/>
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <Style x:Key="MiniSwitch" TargetType="CheckBox">
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="CheckBox">
              <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border x:Name="track" Width="36" Height="18" CornerRadius="9" Background="#55FFFFFF" VerticalAlignment="Center">
                  <Ellipse x:Name="thumb" Width="14" Height="14" Fill="White" HorizontalAlignment="Left" Margin="2,0,0,0"/>
                </Border>
                <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"
                                  TextElement.Foreground="{TemplateBinding Foreground}"/>
              </StackPanel>
              <ControlTemplate.Triggers>
                <Trigger Property="IsChecked" Value="True">
                  <Setter TargetName="track" Property="Background" Value="#EEFFFFFF"/>
                  <Setter TargetName="thumb" Property="Fill" Value="{StaticResource ColorBrush3}"/>
                  <Setter TargetName="thumb" Property="HorizontalAlignment" Value="Right"/>
                  <Setter TargetName="thumb" Property="Margin" Value="0,0,2,0"/>
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
    </ResourceDictionary>
  </Application.Resources>
</Application>
